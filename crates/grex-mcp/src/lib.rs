//! grex-mcp — MCP-native server for grex (M7).
//!
//! Stage 1 scaffold: deps + crate exist and build, but no behavioural code.
//! Replaced in Stage 5 with `GrexMcpServer` + `ServerState`.

pub fn _placeholder() {}
