use crate::cli::args::{GlobalFlags, RunArgs};
use anyhow::Result;

pub fn run(_args: RunArgs, _global: &GlobalFlags) -> Result<()> {
    println!("grex run: unimplemented (M1 scaffold)");
    Ok(())
}
