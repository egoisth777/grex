use crate::cli::args::{GlobalFlags, InitArgs};
use anyhow::Result;

pub fn run(_args: InitArgs, _global: &GlobalFlags) -> Result<()> {
    println!("grex init: unimplemented (M1 scaffold)");
    Ok(())
}
