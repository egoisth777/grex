use crate::cli::args::{AddArgs, GlobalFlags};
use anyhow::Result;

pub fn run(_args: AddArgs, _global: &GlobalFlags) -> Result<()> {
    println!("grex add: unimplemented (M1 scaffold)");
    Ok(())
}
