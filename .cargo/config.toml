# Alias `cargo xtask ...` → `cargo run -p xtask --`. Keeps invocation
# parity with the `cargo-xtask` convention while making the man-page
# generator trivially runnable from CI.
[alias]
xtask = "run --quiet -p xtask --"
