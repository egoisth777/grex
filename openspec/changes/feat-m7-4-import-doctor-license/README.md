# feat-m7-4-import-doctor-license

**Status**: draft

Three M7 deliverables bundled: `grex import --from-repos-json` (legacy `REPOS.json` ingest), `grex doctor` (four-check health probe with `--fix`), and the dual `MIT OR Apache-2.0` licence decision (root licence files + workspace crate metadata + README + CI). Closes the M7 scope items not covered by prior m7 specs; depends on M5 pack-type plugins, M6 managed-gitignore block, and the M3 corruption-resistant manifest reader.
